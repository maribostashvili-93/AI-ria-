// Intentionally insecure fixture file — used to demo `ria security`.
// These are FAKE example values, not real credentials.
export const config = {
  apiKey: "sk_fake_demo_value_1234567890",
  endpoint: "http://api.example.com/v1",
};
